name: Deploy to iFastNet Premium

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.IFASTNET_FTP_SERVER }}
          username: ${{ secrets.IFASTNET_FTP_USERNAME }}
          password: ${{ secrets.IFASTNET_FTP_PASSWORD }}
          protocol: ftp
          # CHANGE THIS LINE: Premium servers push straight to public_html
          server-dir: /public_html/
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/schema.sql
            **/brevo_debug.log
            **/assets/uploads/**